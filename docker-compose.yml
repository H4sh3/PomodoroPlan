version: "3.9"
   
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "127.0.0.1:9050:8000"
    volumes:
      - sqlite_data:/data


volumes:
  sqlite_data: